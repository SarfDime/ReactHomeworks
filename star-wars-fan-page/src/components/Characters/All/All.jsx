import React from 'react'
import { CharacterShell } from '../CharacterShell/CharacterShell'
import { fetchApi } from '../../../shared/sharedFunctions'

export default function All() {
    // const [characters, setCharacters] = useState([])

    // useEffect(async () =>  {
    //     setCharacters(await fetchApi('https://swapi.dev/api/people').results)
    // }, [])

    // return (
    //     <ul>
    //         {characters.map((character) => (
    //             <CharacterShell key={character.url} character={character} />
    //         ))}
    //     </ul>
    // )
}